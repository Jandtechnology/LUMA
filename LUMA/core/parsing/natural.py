from core.ast.base import Program, Node

class NaturalParser:
    def parse(self, source, filename=None) -> Program:
        # Este parser aún no está implementado
        return Program(body=[Node(kind="stub", name="from_natural_parser")])
